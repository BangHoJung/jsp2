package service;

import dao.MemberDAO;
import exception.MemberException;
import vo.MemberVO;

public class MemberService {
	private static MemberService instance = new MemberService();
	
	private MemberService() {
		
	}
	
	public static MemberService getInstance() {
		if(instance == null) instance = new MemberService();
		return instance;
	}
	
	public void insertMemberVO(MemberVO vo) throws Exception {
		if(vo.getId().length() < 1) {
			throw new Exception("아이디를 입력하세요");
		}
		else if(vo.getPass().length() < 8) {
			throw new Exception("비밀번호를 8자이상 입력하세요");
		}
		else if(vo.getName().length()<1) {
			throw new Exception("이름을 입력하세요");
		}
		else if(vo.getAge() < 0 || vo.getAge() > 100) {
			throw new Exception("나이를 올바르게 입력하세요");
		}
		else if() {
			
		}
		else {
			MemberDAO.getIntance().insertMemberVO(vo);
		}
		
	}
	
	public MemberVO searchMemberVO(String id) {
		
		 MemberVO vo = MemberDAO.getIntance().searchMemberVO(id);
		 
		 return vo;
		
	}
	
	public MemberVO searchMemberVO(String id, String name) throws Exception {
		
		 MemberVO vo = MemberDAO.getIntance().searchMemberVO(id,name);
		 
		 return vo;
		
	}
	
	public void updateMemberVO(MemberVO vo) throws MemberException {
		
		MemberDAO.getIntance().updateMemberVO(vo);
	}
	
	public MemberVO checkLoginMember(String id, String pass) {
		return MemberDAO.getIntance().checkLoginMember(id,pass);
	}
}
